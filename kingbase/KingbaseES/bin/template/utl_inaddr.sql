CREATE OR REPLACE PACKAGE UTL_INADDR AUTHID CURRENT_USER AS
	FUNCTION GET_HOST_NAME(ip IN VARCHAR2(128) DEFAULT NULL)
		RETURN VARCHAR2(128);
  FUNCTION GET_HOST_ADDRESS(host IN VARCHAR2(128) DEFAULT NULL)
  	RETURN VARCHAR2(128);
END;
